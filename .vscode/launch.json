{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "cmdline_demo",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/cmake-vs-build-Debug/cmdline_demo",
            "cwd": "${workspaceFolder}",
            "MIMode": "lldb",
        }
    ]
}

